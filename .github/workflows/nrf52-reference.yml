name: nrf52-reference

on:
  push:
    paths:
      - 'firmware/mcu/nordic/nrf52840_dk/**'
      - 'firmware/mcu/common/reference_app/**'
      - 'firmware/core_driver/ads1299/**'
      - 'firmware/common/data_packet/**'
      - '.github/workflows/nrf52-reference.yml'
  pull_request:
    paths:
      - 'firmware/mcu/nordic/nrf52840_dk/**'
      - 'firmware/mcu/common/reference_app/**'
      - 'firmware/core_driver/ads1299/**'
      - 'firmware/common/data_packet/**'
      - '.github/workflows/nrf52-reference.yml'

jobs:
  nrf52840-ncs:
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/nrfconnect/sdk-nrf-toolchain:v3.4.0
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - name: Initialize pinned nRF Connect SDK workspace
        run: |
          west init -m https://github.com/nrfconnect/sdk-nrf --mr v3.4.0 /tmp/ncs
          cd /tmp/ncs
          west update --narrow
          west zephyr-export
      - name: Clean build nRF52840 DK reference
        run: |
          cd /tmp/ncs
          west build -p always --no-sysbuild -b nrf52840dk/nrf52840 "$GITHUB_WORKSPACE/firmware/mcu/nordic/nrf52840_dk"
