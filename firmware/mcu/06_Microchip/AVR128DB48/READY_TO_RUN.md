# AVR128DB48 + ADS1299 Ready-to-Run Guide

This file adds direct-use guidance only. Existing AVR128DB48 files remain unchanged.

## MPLAB / MCC integration

Reuse the current `ads1299_port/` and board support in this directory. Bind the generic callbacks to the SPI/GPIO/interrupt implementation generated by MCC/Melody or used by the XC8 project, then add the shared Microchip ready-app helper and common ADS1299 core/profile sources.

## Peripheral setup

- SPI master, Mode 1 (CPOL=0, CPHA=1), MSB first.
- Start around 1–2 MHz for first hardware validation and increase only after stable frame capture is proven.
- CS, RESET, PWDN and START as outputs.
- DRDY as a falling-edge interrupt input.

## Acquisition flow

Keep the DRDY ISR minimal. Signal a foreground/high-priority acquisition context, read one complete 27-byte ADS1299-8 frame and store it into a static ring buffer. Do not perform floating-point conversion, printf or transport formatting in the ISR.

## Validation sequence

1. Hardware reset ADS1299.
2. Verify device ID.
3. Run `MICROCHIP_ADS1299_PROFILE_INTERNAL_TEST`.
4. Run `MICROCHIP_ADS1299_PROFILE_INPUT_SHORT`.
5. Run `MICROCHIP_ADS1299_PROFILE_EEG_250`.
6. Confirm sustained frame alignment and no silent buffer overflow.
7. Add BIAS, lead-off and faster rates only after baseline stability.

## Resource/transport note

Use static memory and raw integer samples. Verify the UART/USB transport budget before increasing sample rate; expose overflow/read-error counters instead of silently losing frames.

## 64-channel note

For 8 x ADS1299, use FPGA or a higher-throughput multi-SPI capture engine if tight synchronization matters. AVR128DB48 is better suited to control, local monitoring and moderate-throughput transport.

See `../DIRECT_USE.md` for the common Microchip flow.